echo "🔍 Verificando IP actual y accesibilidad..." && echo "" && echo "1. IP actual del contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "2. Probar conexión desde Traefik con IP actual:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$CURRENT_IP:3000 2>&1 | head -5 && echo "" && echo "3. Ver todas las IPs del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{range .Endpoint.VirtualIPs}}{{.Addr}} {{end}}' && echo "" && echo "4. Agregar alias al servicio:" && docker service update --network-rm easypanel --network-add name=easypanel,alias=checkin24hs-dashboard checkin24hs_checkin24hs-dashboard && echo "" && echo "5. Esperar 10 segundos..." && sleep 10 && echo "" && echo "6. Verificar nuevo alias:" && NEW_CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker inspect $NEW_CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}Aliases: {{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}'

