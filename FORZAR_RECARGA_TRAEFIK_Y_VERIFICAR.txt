echo "🔧 Forzando recarga de Traefik y verificando..." && echo "" && echo "1. Probar resolución del nombre del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "2. Recargar configuración de Traefik:" && docker exec $(docker ps | grep traefik | awk '{print $1}') kill -HUP 1 && echo "✅ Traefik recargado" && echo "" && echo "3. Esperar 5 segundos..." && sleep 5 && echo "" && echo "4. Verificar logs recientes de Traefik:" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 10 | grep -i -E "(panel|checkin24hs|reload)" || echo "No se encontraron logs relevantes"

