echo "🔍 Verificando alias del contenedor dashboard..." && echo "" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && echo "ID del contenedor: $CONTAINER_ID" && echo "" && echo "Alias del contenedor en la red easypanel:" && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "IP del contenedor:" && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}' && echo "" && echo "Probando conexión desde Traefik con el alias encontrado:" && ALIAS=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{range $value.Aliases}}{{.}}{{break}}{{end}}{{end}}{{end}}') && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$ALIAS:3000 2>&1 | head -10

