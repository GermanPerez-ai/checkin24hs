echo "🔍 Verificando configuración de Traefik para panel.checkin24hs.com..." && echo "" && echo "1. Probar resolución del nombre del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "2. Ver configuración de Traefik (routers y services):" && docker exec $(docker ps | grep traefik | awk '{print $1}') cat /etc/traefik/dynamic/*.yml 2>/dev/null | grep -A 20 -i "panel.checkin24hs.com" || echo "No se encontró configuración específica" && echo "" && echo "3. Ver todos los routers configurados:" && docker exec $(docker ps | grep traefik | awk '{print $1}') cat /etc/traefik/dynamic/*.yml 2>/dev/null | grep -i "rule:" | head -10 || echo "No se encontraron routers" && echo "" && echo "4. Ver logs de Traefik (últimas 30 líneas):" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 30

