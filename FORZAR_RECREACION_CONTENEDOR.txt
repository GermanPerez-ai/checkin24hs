echo "🔧 Forzando recreación del contenedor..." && echo "" && echo "1. Escalar a 0:" && docker service scale checkin24hs_checkin24hs-dashboard=0 && echo "Esperando 10 segundos..." && sleep 10 && echo "" && echo "2. Escalar a 1:" && docker service scale checkin24hs_checkin24hs-dashboard=1 && echo "Esperando 20 segundos..." && sleep 20 && echo "" && echo "3. Verificar nuevo contenedor y alias:" && NEW_CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && echo "ID: $NEW_CONTAINER_ID" && docker inspect $NEW_CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}IP: {{$value.IPAddress}}, Aliases: {{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "4. Probar con alias:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "5. Probar con nombre completo del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -5

