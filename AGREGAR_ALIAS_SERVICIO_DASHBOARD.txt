echo "🔧 Agregando alias 'checkin24hs-dashboard' al servicio..." && echo "" && docker service update --network-add name=easypanel,alias=checkin24hs-dashboard checkin24hs_checkin24hs-dashboard && echo "" && echo "Esperando 5 segundos para que se aplique..." && sleep 5 && echo "" && echo "Verificando que el alias se agregó:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "Probando conexión con el alias:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs-dashboard:3000 2>&1 | head -10

