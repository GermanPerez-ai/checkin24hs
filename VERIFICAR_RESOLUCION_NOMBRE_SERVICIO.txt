echo "🔍 Verificando resolución del nombre del servicio..." && echo "" && echo "1. Probar con nombre completo del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "2. Verificar configuración de Traefik para panel.checkin24hs.com:" && docker exec $(docker ps | grep traefik | awk '{print $1}') cat /etc/traefik/dynamic/*.yml 2>/dev/null | grep -A 10 -i panel || echo "No se encontró configuración específica" && echo "" && echo "3. Ver logs de Traefik para errores:" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 30 | grep -i -E "(panel|checkin24hs|error|502)" || echo "No se encontraron errores relacionados"

