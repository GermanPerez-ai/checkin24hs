echo "🔍 Verificando resolución del nombre del servicio..." && echo "" && echo "1. Probar con nombre completo:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "2. Verificar si el servicio tiene alias en la red:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}Red: {{$key}}, IP: {{$value.IPAddress}}, Aliases: {{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "3. Verificar configuración de red del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{json .Spec.TaskTemplate.Networks}}' | jq

