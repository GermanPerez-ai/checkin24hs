echo "🔍 Verificando configuración de Traefik..." && echo "" && echo "1. Ver configuración de Traefik para panel.checkin24hs.com:" && docker exec $(docker ps | grep traefik | awk '{print $1}') cat /etc/traefik/dynamic/*.yml 2>/dev/null | grep -A 20 -i "panel.checkin24hs.com" || echo "No se encontró en archivos yml" && echo "" && echo "2. Ver todos los routers:" && docker exec $(docker ps | grep traefik | awk '{print $1}') cat /etc/traefik/dynamic/*.yml 2>/dev/null | grep -B 5 -A 15 "panel" || echo "No se encontró" && echo "" && echo "3. IP actual del contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "4. Probar conexión con IP actual:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$CURRENT_IP:3000 2>&1 | head -3

