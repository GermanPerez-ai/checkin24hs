echo "🔍 DIAGNÓSTICO COMPLETO DEL ESTADO..." && echo "" && echo "1. Estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "2. Ver contenedores corriendo:" && docker ps | grep checkin24hs_checkin24hs-dashboard && echo "" && echo "3. IP actual del contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "4. Probar conexión local desde el servidor:" && curl -I http://localhost:30002 2>&1 | head -5 && echo "" && echo "5. Probar conexión con IP del contenedor:" && curl -I http://$CURRENT_IP:3000 2>&1 | head -5 && echo "" && echo "6. Ver logs del servicio:" && docker service logs checkin24hs_checkin24hs-dashboard --tail 10 && echo "" && echo "7. Ver puertos publicados:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{json .Endpoint.Ports}}' | jq

