echo "🔍 Verificando configuración final..." && echo "" && echo "1. Ver configuración de red del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{json .Spec.TaskTemplate.Networks}}' | jq && echo "" && echo "2. Probar con IP directa:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://10.11.131.96:3000 2>&1 | head -3 && echo "" && echo "3. Ver logs de Traefik para ver qué está pasando:" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 20 | grep -i -E "(panel|checkin24hs|error)" || echo "No se encontraron errores relevantes"

