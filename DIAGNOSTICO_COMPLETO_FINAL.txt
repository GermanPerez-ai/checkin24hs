echo "🔍 DIAGNÓSTICO COMPLETO..." && echo "" && echo "1. Estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "2. IP actual:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "3. Probar conexión desde Traefik con IP:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$CURRENT_IP:3000 2>&1 | head -3 && echo "" && echo "4. Ver logs del servicio dashboard:" && docker service logs checkin24hs_checkin24hs-dashboard --tail 5 && echo "" && echo "5. Ver logs de Traefik (últimas 30 líneas):" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 30 && echo "" && echo "6. Verificar que Traefik está en la red easypanel:" && docker inspect $(docker ps | grep traefik | awk '{print $1}') --format '{{range $key, $value := .NetworkSettings.Networks}}{{$key}} {{end}}'

