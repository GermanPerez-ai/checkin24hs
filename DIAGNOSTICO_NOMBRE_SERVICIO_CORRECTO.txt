echo "==========================================" && echo "🔍 DIAGNÓSTICO CON NOMBRE CORRECTO" && echo "==========================================" && echo "" && echo "1️⃣ Red del servicio dashboard:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{range .Spec.TaskTemplate.Networks}}{{.Target}} {{end}}' | xargs -I {} docker network inspect {} --format '{{.Name}}' && echo "" && echo "2️⃣ IP del servicio:" && DASHBOARD_IP=$(docker service inspect checkin24hs_checkin24hs-dashboard --format '{{range .Endpoint.VirtualIPs}}{{.Addr}} {{end}}' | awk '{print $1}' | cut -d'/' -f1) && echo "IP: $DASHBOARD_IP" && echo "" && echo "3️⃣ Prueba con nombre completo del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -10 && echo "" && echo "4️⃣ Prueba con IP directa:" && if [ ! -z "$DASHBOARD_IP" ]; then docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$DASHBOARD_IP:3000 2>&1 | head -10; else echo "❌ No se pudo obtener la IP"; fi && echo "" && echo "5️⃣ Estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "6️⃣ Últimos logs:" && docker service logs checkin24hs_checkin24hs-dashboard --tail 5

