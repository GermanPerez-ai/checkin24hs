echo "🔧 Agregando alias al servicio que ya está en la red..." && echo "" && echo "1. Removiendo la red actual (sin alias):" && docker service update --network-rm easypanel checkin24hs_checkin24hs-dashboard && echo "" && echo "Esperando 3 segundos..." && sleep 3 && echo "" && echo "2. Agregando la red con el alias:" && docker service update --network-add name=easypanel,alias=checkin24hs-dashboard checkin24hs_checkin24hs-dashboard && echo "" && echo "Esperando 5 segundos para que se aplique..." && sleep 5 && echo "" && echo "3. Verificando que el alias se agregó:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && echo "ID del contenedor: $CONTAINER_ID" && echo "Alias:" && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "4. Probando conexión con el alias:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs-dashboard:3000 2>&1 | head -10

