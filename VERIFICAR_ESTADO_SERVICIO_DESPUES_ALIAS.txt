echo "🔍 Verificando estado del servicio después del cambio..." && echo "" && echo "1. Estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "2. IP actual del contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && echo "ID: $CONTAINER_ID" && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}' && echo "" && echo "3. Redes del contenedor:" && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{$key}}: {{range $value.Aliases}}{{.}} {{end}}{{end}}' && echo "" && echo "4. Probando con IP directa:" && NEW_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$NEW_IP:3000 2>&1 | head -10

