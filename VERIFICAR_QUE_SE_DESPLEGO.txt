echo "🔍 Verificando qué se desplegó..." && echo "" && echo "1. Ver imagen del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{.Spec.TaskTemplate.ContainerSpec.Image}}' && echo "" && echo "2. Ver archivos en el contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker exec $CONTAINER_ID ls -la /app/ | head -20 && echo "" && echo "3. Verificar si existe dashboard.html:" && docker exec $CONTAINER_ID ls -la /app/dashboard.html && echo "" && echo "4. Verificar si existe checkin24hs-admin:" && docker exec $CONTAINER_ID ls -la /app/checkin24hs-admin 2>&1 | head -5 || echo "No existe checkin24hs-admin" && echo "" && echo "5. Ver logs del servicio:" && docker service logs checkin24hs_checkin24hs-dashboard --tail 10

