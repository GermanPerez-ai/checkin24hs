echo "🔍 Verificando conectividad del dominio panel.checkin24hs.com..." && echo "" && echo "1. Estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "2. IP actual del contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "3. Probando conexión desde Traefik con nombre del servicio:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_checkin24hs-dashboard:3000 2>&1 | head -10 && echo "" && echo "4. Probando conexión desde Traefik con IP directa:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$CURRENT_IP:3000 2>&1 | head -10 && echo "" && echo "5. Verificando logs del servicio:" && docker service logs checkin24hs_checkin24hs-dashboard --tail 5

