echo "🔍 Diagnóstico final..." && echo "" && echo "1. Ver logs de Traefik (últimas 30 líneas):" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 30 | grep -i -E "(panel|checkin24hs|error|502|bad)" || echo "No se encontraron errores relevantes" && echo "" && echo "2. Ver todos los logs recientes de Traefik:" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 10 && echo "" && echo "3. IP actual del servicio:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "4. Probar conexión directa desde el host:" && curl -I http://$CURRENT_IP:3000 2>&1 | head -3 && echo "" && echo "5. Ver estado del servicio:" && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3

