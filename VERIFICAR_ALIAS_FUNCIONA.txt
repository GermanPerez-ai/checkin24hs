echo "🔍 Verificando si el alias funciona..." && echo "" && echo "1. Probar con alias checkin24hs-dashboard:" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "2. Verificar contenedor actual:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}IP: {{$value.IPAddress}}, Aliases: {{range $value.Aliases}}{{.}} {{end}}{{end}}{{end}}' && echo "" && echo "3. Verificar resolución DNS desde Traefik:" && docker exec $(docker ps | grep traefik | awk '{print $1}') nslookup checkin24hs-dashboard 2>&1 | head -10 || docker exec $(docker ps | grep traefik | awk '{print $1}') getent hosts checkin24hs-dashboard 2>&1

