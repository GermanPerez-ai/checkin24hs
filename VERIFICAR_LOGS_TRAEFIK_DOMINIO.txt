echo "🔍 Verificando logs de Traefik..." && echo "" && echo "1. Ver logs recientes de Traefik (últimas 50 líneas):" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 50 | grep -i -E "(panel|checkin24hs|error|502|bad|gateway)" || echo "No se encontraron errores relacionados" && echo "" && echo "2. Ver TODOS los logs recientes:" && docker logs $(docker ps | grep traefik | awk '{print $1}') --tail 30 && echo "" && echo "3. IP actual del servicio:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && CURRENT_IP=$(docker inspect $CONTAINER_ID --format '{{range $key, $value := .NetworkSettings.Networks}}{{if eq $key "easypanel"}}{{$value.IPAddress}}{{end}}{{end}}') && echo "IP: $CURRENT_IP" && echo "" && echo "4. Verificar configuración de red del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{json .Spec.TaskTemplate.Networks}}' | jq

