echo "🔄 Forzando reconstrucción completa del servicio..." && echo "" && echo "1️⃣ Escalando a 0 (detener)..." && docker service scale checkin24hs_checkin24hs-dashboard=0 && sleep 10 && echo "" && echo "2️⃣ Forzando actualización del servicio..." && docker service update --force checkin24hs_checkin24hs-dashboard && sleep 5 && echo "" && echo "3️⃣ Escalando a 1 (iniciar)..." && docker service scale checkin24hs_checkin24hs-dashboard=1 && sleep 15 && echo "" && echo "4️⃣ Verificando estado..." && docker service ps checkin24hs_checkin24hs-dashboard --no-trunc | head -3 && echo "" && echo "5️⃣ Verificando logs..." && docker service logs checkin24hs_checkin24hs-dashboard --tail 10

