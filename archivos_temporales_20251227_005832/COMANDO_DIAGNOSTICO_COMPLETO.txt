echo "==========================================" && echo "🔍 DIAGNÓSTICO COMPLETO DEL DASHBOARD" && echo "==========================================" && echo "" && echo "1️⃣ Red del servicio dashboard:" && docker service inspect checkin24hs_dashboard --format '{{range .Spec.TaskTemplate.Networks}}{{.Target}} {{end}}' | xargs -I {} docker network inspect {} --format '{{.Name}}' && echo "" && echo "2️⃣ Red de Traefik:" && docker inspect $(docker ps | grep traefik | awk '{print $1}') --format '{{range $key, $value := .NetworkSettings.Networks}}{{$key}} {{end}}' && echo "" && echo "3️⃣ IP del servicio:" && DASHBOARD_IP=$(docker service inspect checkin24hs_dashboard --format '{{range .Endpoint.VirtualIPs}}{{.Addr}} {{end}}' | awk '{print $1}' | cut -d'/' -f1) && echo "IP: $DASHBOARD_IP" && echo "" && echo "4️⃣ Prueba con alias (guión):" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs-dashboard:3000 2>&1 | head -5 && echo "" && echo "5️⃣ Prueba con nombre servicio (guión bajo):" && docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://checkin24hs_dashboard:3000 2>&1 | head -5 && echo "" && echo "6️⃣ Prueba con IP directa:" && if [ ! -z "$DASHBOARD_IP" ]; then docker exec $(docker ps | grep traefik | awk '{print $1}') wget -O- http://$DASHBOARD_IP:3000 2>&1 | head -5; else echo "❌ No se pudo obtener la IP"; fi && echo "" && echo "7️⃣ Contenedores en red easypanel:" && docker network inspect easypanel --format '{{range .Containers}}{{.Name}} {{end}}' 2>/dev/null | grep -i dashboard || echo "❌ No encontrado" && echo "" && echo "8️⃣ Estado del servicio:" && docker service ps checkin24hs_dashboard --no-trunc | head -3 && echo "" && echo "9️⃣ Últimos logs:" && docker service logs checkin24hs_dashboard --tail 5

