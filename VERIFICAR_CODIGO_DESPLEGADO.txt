echo "🔍 Verificando qué código está desplegado..." && echo "" && echo "1. Ver imagen del servicio:" && docker service inspect checkin24hs_checkin24hs-dashboard --format '{{.Spec.TaskTemplate.ContainerSpec.Image}}' && echo "" && echo "2. Ver logs del servicio (últimas 20 líneas):" && docker service logs checkin24hs_checkin24hs-dashboard --tail 20 && echo "" && echo "3. Verificar archivos en el contenedor:" && CONTAINER_ID=$(docker ps | grep checkin24hs_checkin24hs-dashboard | awk '{print $1}' | head -1) && docker exec $CONTAINER_ID ls -la /app/build/ 2>/dev/null | head -10 || echo "No se encontró directorio build" && echo "" && echo "4. Verificar index.html:" && docker exec $CONTAINER_ID cat /app/build/index.html 2>/dev/null | head -20 || echo "No se encontró index.html"

